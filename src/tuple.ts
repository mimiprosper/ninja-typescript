//tuples - positions are fixed, can't be changed, unlike an array
let student: [string, number];
student = ["james", 40]; // position student[0] must be = string, position stident[1] must be = number
